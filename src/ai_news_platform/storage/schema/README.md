# SQLite Schema (placeholder)

Schema is designed to support:

- Source metadata persistence and monitoring fields
- Article lifecycle states: raw → normalized → rewritten → published
- Processing status and publication status
- Deduplication enforced via database constraints

Apply migrations/DDL in a future implementation step.

